timezone: 'Asia/Tokyo'

_export:
  !include : 'conf/files.dig'
  !include : 'conf/mail.dig'

+load:
  _parallel: true

  for_each>:
    datum: ${files}
  _do:
    sh>: embulk run embulk/load_${datum}.yml.liquid
    _env:
      file_date: ${moment(session_time).add(-1, 'days').format("YYYYMMDD")}
